spring.application.name=app
#Server port
server.port = 8080 

# View resolver setting (not use) 
# spring.mvc.view.prefix=/WEB-INF/views/
# spring.mvc.view.suffix=.jsp

#JDBC 연동
spring.datasource.url=jdbc:mysql://localhost:3306/vmsdb?useUnicode=true&serverTimezone=Asia/Seoul
spring.datasource.username=root
spring.datasource.password=12345678
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
 
#AWS RDS 연동(Elastic beanstalk) 
# spring.datasource.url=jdbc:mysql://vmsdb.ce4abl7nyur3.ap-northeast-2.rds.amazonaws.com:3306/vmsdb
# spring.datasource.username=root
# spring.datasource.password=vmsfighting
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.datasource.driver-class-name=com.mysql.jdbc.Driver

#JPA
# 사용할 데이터베이스 종류 지정 (mysql, h2 등)
spring.jpa.database=mysql
# Hibernate가 사용할 DB 방언(Dialect) 지정
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect

# 애플리케이션 실행 시 DDL 자동 생성/수정 옵션 (none, update, create, create-drop 등)
spring.jpa.hibernate.ddl-auto=update
# Hibernate의 로그 레벨 설정 (info, debug, error 등)
logging.level.org.hibernate=info
# 실행되는 SQL 쿼리를 콘솔에 출력
spring.jpa.properties.hibernate.show_sql=true
# SQL 쿼리를 보기 좋게 포맷팅해서 출력
spring.jpa.properties.hibernate.format_sql=true
# SQL 쿼리에 주석 추가해서 출력
spring.jpa.properties.hibernate.use_sql_comments=true
# SQL 파라미터 바인딩 값까지 로그로 출력
logging.level.org.hibernate.type.descriptor.sql=trace

# 실제 DB에 적용될 물리적 네이밍 전략 지정
spring.jpa.hibernate.naming.physical-strategy=org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy
# Open EntityManager in View 패턴 비활성화(화며단에서의 DB접근 제한) (권장: false)
spring.jpa.open-in-view=false

#########Log Setting#########
#logging.level.root=info  # 전체 로그 레벨 설정
#logging.level.com.finalTest.lins.controller=info  # 특정 패키지(컨트롤러)의 로그 레벨 설정
#logging.file.name=myLog.log  # 로그 파일 이름 지정
# logging.file.path=C:/Users/ksygt/Desktop/logTest/  # 로그 파일 저장 경로 지정
# spring.main.banner-mode=off  # Spring Boot 실행 배너 출력 여부 (off: 출력 안 함)
# spring.output.ansi.enabled=always  # 콘솔 로그에 ANSI 컬러 항상 사용
#logging.level.org.springframework.web=debug  # Spring Web 관련 로그 레벨 debug로 설정
#logging.level.org.hibernate=error  # Hibernate 로그 레벨 error로 설정

# spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true  # 트랜잭션 없이 LAZY 로딩 허용 (권장X)
