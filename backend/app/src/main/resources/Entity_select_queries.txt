
/*
 * 하이버네이트가 순서대로 컬럼 생성을 하지 않아서 순서대로 보이는 쿼리 
 *
 * */

-- TB_BBS (게시판 테이블)
SELECT
  BBS_ID,
  BBS_TYPE,
  TITLE,
  CONTENT,
  WRITOR,
  WRITE_DATE,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_BBS;

-- TB_BBS_COMMENT (게시판 댓글 테이블)
SELECT
  COMMENT_ID,
  BBS_ID,
  CONTENT,
  WRITOR,
  WRITE_DATE,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_BBS_COMMENT;

-- TB_CHAEBUN (채번(시퀀스) 테이블)
SELECT
  SEQ_ID,
  SEQ_NAME,
  PREFIX,
  CURRENT_VALUE,
  STEP,
  LENGTH,
  DATEFORMAT,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_CHAEBUN;

-- TB_COM_CODE_D (공통코드 상세 테이블)
SELECT
  GRP_CD,
  ATTR_CD,
  DTL_CD,
  DTL_NM,
  USE_YN,
  ORDER_NUM,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_COM_CODE_D;

-- TB_COM_CODE_M (공통코드 마스터 테이블)
SELECT
  GRP_CD,
  GRP_CD_TYPE,
  GRP_NM,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_COM_CODE_M;

-- TB_COM_CODE_T (공통코드 속성 테이블)
SELECT
  GRP_CD,
  ATTR_CD,
  ATTR_NM,
  ORDER_NUM,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_COM_CODE_T;

-- TB_COMPANY (회사 테이블)
SELECT
  COMPANY_CODE,
  COMPANY_NAME,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_COMPANY;

-- TB_DEPARTMENT (부서 테이블)
SELECT
  DEPT_CODE,
  DEPT_NM,
  UPPER_DEPT_CODE,
  DEPT_LV,
  COMPANY_CODE,
  USE_YN,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_DEPARTMENT;

-- TB_IF (인터페이스 테이블)
SELECT
  IF_ID,
  IF_NAME,
  TEXT,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_IF;

-- TB_LOG_ACT (사용자 행위 로그 테이블)
SELECT
  LOG_ID,
  USER_ID,
  IP_ADDR,
  PAGE_URL,
  ACTION_TYPE,
  ACTION_TYPE_DETAIL,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_LOG_ACT;

-- TB_LOG_API (API 호출 로그 테이블)
SELECT
  LOG_ID,
  USER_ID,
  IP_ADDR,
  USER_AGENT,
  REQUEST_URI,
  HTTP_METHOD,
  REQUEST_BODY,
  RESPONSE_BODY,
  STATUS_CODE,
  EXEC_TIME,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_LOG_API;

-- TB_LOG_ERROR (에러 로그 테이블)
SELECT
  ERR_ID,
  USER_ID,
  IP_ADDR,
  USER_AGENT,
  REQUEST_URI,
  HTTP_METHOD,
  ERR_MSG,
  ERR_STACK,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_LOG_ERROR;

-- TB_MAIL_H (메일 발송 이력 테이블)
SELECT
  LOG_ID,
  MAIL_ID,
  FROM_ADDR,
  TO_ADDR,
  TITLE,
  CONTENT,
  SUCCESS,
  ERROR_MSG,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_MAIL_H;

-- TB_MAIL_M (메일 템플릿 테이블)
SELECT
  MAIL_ID,
  LANG_TYPE,
  MAIL_NAME,
  TITLE,
  CONTENT,
  DESCRIPTION,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_MAIL_M;

-- TB_MENU (메뉴 테이블)
SELECT
  MENU_CD,
  MENU_NM,
  UPPER_MENU_CD,
  MENU_LV,
  USE_YN,
  MENU_URL,
  ORDER_NUM,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_MENU;

-- TB_MUL_LANG (다국어 테이블)
SELECT
  LANG_CD,
  LANG_TYPE,
  LANG_NM,
  LANG_GUBUN,
  USE_YN,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_MUL_LANG;

-- TB_NOTICE (공지 테이블)
SELECT
  NOT_ID,
  NOT_TYPE,
  TITLE,
  CONTENT,
  WRITOR,
  WRITE_DATE,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_NOTICE;

-- TB_ROLE (권한 테이블)
SELECT
  ROLD_CD,
  ROLE_NAME,
  ROLE_DESC,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_ROLE;

-- TB_ROLE_MENU (권한-메뉴 매핑 테이블)
SELECT
  ROLD_CD,
  MENU_CD,
  MENU_RW,
  USE_YN,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_ROLE_MENU;

-- TB_ROLE_USER (권한-사용자 매핑 테이블)
SELECT
  USER_ID,
  ROLD_CD,
  USE_YN,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_ROLE_USER;

-- TB_SCHE_H (스케줄 실행 이력 테이블)
SELECT
  LOG_ID,
  SCHE_ID,
  START_TIME,
  END_TIME,
  EXEC_TIME,
  SUCCESS,
  ERROR_MSG,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_SCHE_H;

-- TB_SCHE_M (스케줄 마스터 테이블)
SELECT
  SCHE_ID,
  SCHE_NAME,
  DESCRIPTION,
  CRON_EXP,
  LAST_EXEC_TIME,
  USE_YN,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_SCHE_M;

-- TB_SMS_H (SMS 발송 이력 테이블)
SELECT
  LOG_ID,
  SMS_ID,
  FROM_PHONE,
  TO_PHONE,
  TEXT,
  SUCCESS,
  ERROR_MSG,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_SMS_H;

-- TB_SMS_M (SMS 템플릿 테이블)
SELECT
  SMS_ID,
  LANG_TYPE,
  SMS_NAME,
  TEXT,
  DESCRIPTION,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_SMS_M;

-- TB_USER (사용자 테이블)
SELECT
  USER_ID,
  PASSWORD,
  NAME,
  PHONE_NUM,
  EMAIL,
  ROLE,
  USER_TYPE,
  GENDER,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_USER;

-- TB_USER_AUTH (사용자 인증 테이블)
SELECT
  USER_ID,
  REFRESH_TOKEN,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_USER_AUTH;

-- TB_WEB_SVC (웹서비스 테이블)
SELECT
  SVC_ID,
  SVC_NAME,
  STS,
  CREATE_USER,
  CREATE_DATE,
  UPDATE_USER,
  TIMESTAMP
FROM TB_WEB_SVC; 